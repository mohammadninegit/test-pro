>>>توضیحات گیت

از نظر گیت 2 دسته فایل داریم
tracked
اینکه فایل ها مارو دنبال میکنه و یک تاریخچه از فایل هامون داریم tracked منظور از
untracked
حالا فایل های ما یک ترک میشن یا ان ترک

git -v
این دستور اگر ورزن گیت رو به درستی نشون داد یعنی به درستی برنامه نصب شده


هرجا خواستیم گیت ایجاد کنیم با کلیک راسه به گزینه
دسترسی داریم و ایجادش میکنیم git bash here

فرض کنیم گروهی داریم رو یک پروزه کار میکنیم
وظیفه گیت اینکه مشخص کنه چه کسی در چه تاریخی چه تغییراتی در پروزه ایجاد کرده

اول باید خودمون رو به گیت معرفی کنیم با این دستور

git config-- global user.name "m.h9"

--global 
یعنی هرجا هرپروزه ای با استفاده از سیستم من ساخته شه گیت من رو به اسمی که گفتم میشناسه

git config-- global user.email "m.h9payam@gmail.com"


working directory | staging area | git repository


working directory
همون مسیری که ما فایل های پوشه مون رو داخلش قرار دادیم directory
مسیری که داریم کار میکنیم روش


بعد از ایجاد فایل
باید انتقالش بدیم به محیط استیج
staging area
git add با استفاده از دستور


بعد باید فایل رو وارد مخزن گیت کنیم
git repository
git commit با استفاده از دستور

میتونیم به راحتی به تمام ورزن های  پروزه مون دسترسی داشته باشیم



از طریق ترمینال وی اس کد میتونیم دستورات گیت وارد کنیم

دستور برای ایجاد کردن مخزن git init 

وقتی یک فایلی رو تازه ایجاد میکنیم ان ترک هستیم
هست و هنوز وارد استیج نشده working directory چون


git log
این دستور وضعیت دستورات پروزه رو بهم نشون میده

git log --oneline
این دستور باعث میشه گزارشات رو مرتب تر دریاف کنیم

modifed 
یعنی تغییرات داشته


نکته
قبل اینکه کامیت بزنیم باید تغییرات رو وارد استیج کنیم



خیلی راحت میشه پروزه رو برگردونیم به حالت قبلی و دستورات جدید رو نمیخوایم باشه
با دستور git checkout + شناسه منحصر به فرد کامیت
هر کامیت شناسه منحصر به فرد خودشو داره git log --oneline در درستور 

git checkout master
برمیگرده به اخرین تغییراتی که داشتیم






>>>git add .<<<
به طور مثال فرضا ده تا فایل داریم میخوایم همه رو اضافه کنیم به استیج
به جای اینکه همه رو یکی یکی بخوایم کامیت کنیم از دستور اضافه کردن همه استفاده میکنیم
git add .
به معنای همه هست .


فایل ها رو به استیج اضافه کنیم بعد کامیت کنیم git add قبل از کامیت کردن باید 




//////////////////////////////////////
[جلسه پنجم]


دو نوع مخزن داریم

مخزن محلی          local repository
مخزن راه دور       remote repository


local repository 
همون مخزنی هست که روی سیستم خودمون اجرا کردیم


remote repository 
مخزنی هست که روی سرویس های گیت هاب یا گیت لب میسازیم
و غیر از خودم هر کسی میتونه بهش دسترسی داشته باشه

طبق توضیحات داده شده بعد از ایجاد مخزن محلی برای اتصال مخزن محلی به راه دور
از دستور پایین استفاده میکنیم

git remote add origin https://github.com/mohammadninegit/test-pro.git

اگر میخوایم دستوراتی رو که در مخزن محلی داریم رو منتقل کنیم به مخزن راه دور
و منتقل کنیم به گیت هاب از دستور زیر استفاده میکنیم


git push

push به معنی هل دادن استفاده
وقتی یک سری تغییرات بوجود میاریم بعد میخوایم کامیت ثبت کنیم  و میخوایم تغییرات
جدید رو بفرستیم روی گیت هاب از دستور  
استفاده میکنیم git push




git pull
به معنی واکشی کردن است

به طور مثال چند نفر داریم روی یک پروزه کار میکنیم حالا من میخوام رو پروزه کار کنم
استفاده میکنیم git pull برای اینکه اخرین تغییرات رو از گیت هاب بگیرم از دستور

git pull origin master
 همیشه قبل از شروع کردن پروزه مشترک با این دستور اخرین تغییرات پروزه رو دریافت کنید
 
